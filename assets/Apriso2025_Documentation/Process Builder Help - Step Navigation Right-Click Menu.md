# Step Navigation Right-Click Menu

Step Navigation Workspace

The right-click menu can be accessed by using the context menu keyboard key or by right-clicking
anywhere in the Step Navigation Workspace.

                   Available options:

                   Command     Description
                   Add Step    Adds a new step to the step navigation.
                   Add         Displays the list of DFCs from which you
                   Existing    can choose an existing step.
                   Step
                   Add         Adds a Decision Step (see Step
                   Decision    Navigation Diagram Elements).
                   Step
                   Add Step    Opens a wizard with DFCs to choose
                   with Sub-   from (see Create Step with Sub-DFC
                   DFC         Wizard).
                   Add Stop    Adds a stop node to the diagram.
                   Node
                   Paste       Pastes the Step from the clipboard.
                   Autolayout  Automatically arranges all of the items in
                               the diagram (this option is also available
                   Save As     when an entity is in the active status).
                   Image       Saves a flow diagram as an image (this
                               option is also available when an entity is
                   Properties  in the active status). An alternative
                               method here is to select entities and
                               press Ctrl+C – the selected Steps will
                               then be copied to the clipboard as an
                               image (which can be pasted into
                               Microsoft Word or another application).
                               Opens the Properties tool pane of the
                               DFC (this option is also available when
                               an entity is in the active status).

Step

 Connector Line    Command      Description
                   Rename
                   Set Routing  Changes the name of the selected
                   Output       step.
                   Edit
                   Description  Enables setting a function output that
                   Show/Hide    will be used as an output routing of a
                   Step         DFC step.
                   Description
                   Cut          Enables changing the step description.

                   Copy         Toggles the visibility of the step
                   Paste        description.

                   Delete       Copies one or more selected steps to
                                the clipboard and removes them from
                   Duplicate    the entity workspace and the Entity
                   Selection    Explorer tree.
                   Autolayout
                   Save As      Copies one or more selected steps to
                   Image        the clipboard.

                   Properties   Inserts the last copied step from the
                                clipboard into the entity workspace and
                                the Entity Explorer tree .

                                Deletes the selected step. When an
                                arrow is selected, removing it also
                                removes the routing path.

                                Available only when a step is selected.
                                You can select one step for duplication.

                                Automatically arranges all of the items
                                in the diagram.

                                Saves a flow diagram as an image. An
                                alternative method here is to select
                                entities and press Ctrl+C – the
                                selected steps will then be copied to
                                clipboard as an image (which can be
                                pasted into Microsoft Word or another
                                application).

                                Gives focus to the Properties Tool Pane
                                for the selected step.

                   Command Description

                   Change       Enables changing the routing value for

                   Routing Value the selected path.

                   Delete       Deletes the selected arrow. When a
                                step is selected, removing it also
                                removes the step.

 Sub-DFC Workspace (for Non-Technical User Roles)

   Command          Description
   View             Opens a sub-DFC in read-only mode.
   Edit in new tab  Opens a sub-DFC in a new tab.
