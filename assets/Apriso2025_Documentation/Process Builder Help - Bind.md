# Bind

Bind is used for binding an HTML element to the Screen Interface Output.

<htmltag data-flx-bind = "[Output name]"></htmltag>

Inputs/Outputs                       HTML Layout Editor/HTML Editor       Runtime
           DFC Function Navigation:

                                     HTML code

HTML View:

The Bind converter cannot be used with the Complex data type.
