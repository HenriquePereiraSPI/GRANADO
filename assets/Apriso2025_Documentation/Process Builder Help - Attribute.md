# Attribute

<data-flx-attr = "[attribute]">

During runtime, the whole expression is replaced with the attribute value.

Inputs                               HTML Layout Editor/HTML Editor         Runtime
           DFC Function Navigation:

                                     HTML code

HTML View:
